import React from 'react';
import ReactDOM from 'react-dom';
import StudentContainer from './components/StudentContainer';

//const element = <h1>Welcome to the the torilo student mgt app!</h1>
ReactDOM.render(
<React.StrictMode>
    <StudentContainer />
</React.StrictMode>,
 document.getElementById('root'));
